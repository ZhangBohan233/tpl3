
fn t2(x: int, y: int) int {
    return x + y;
}

fn t1(n: int) int {
    return t2(n, n + 1);
}

fn fib(n: int) int {
    //printf("%d", &n);
    if n < 2 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

fn main() int {
    var a: int = fib(7);
    return a;
}