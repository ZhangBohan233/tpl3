
fn add(x: int, y: int) int {
    return x + y;
}

fn ori(x: int) int {
    return x;
}

fn test(x: int) int {
    var y: int;
    if x == 5 {
        y = 10;
    } else {
        y = 1;
    }
    return y;
}

fn main() int {
    var t0: int = clock();
    var a: int = test(4);
    var b: int = a + 5;
    var t1: int = clock();
    return t1 - t0;
}