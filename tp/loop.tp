fn main() int {

    const t1: int = clock();

    for var i: int = 0; i < 1000; i += 1 {
        //printf("%d", i);
    }

    var i: int = 0;
    while i < 1000 {
        i += 1;
    }

    //memory_view();

    const t2: int = clock();
    printf("%d", t2 - t1);

    return 0;
}